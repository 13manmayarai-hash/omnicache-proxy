apiVersion: v1
kind: Service
metadata:
  name: {{ include "omnicache.fullname" . }}
  labels:
    {{- include "omnicache.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "omnicache.selectorLabels" . | nindent 4 }}
